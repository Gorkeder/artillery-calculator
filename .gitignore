# Расшифрованная карта — НИКОГДА не коммитить вручную до релиза.
# В день релиза её кладёт в репозиторий GitHub Action (или см. RELEASE.md).
map.js
.env

# Локальная копия для ручного превью ленты видео — генерируется на VPS, не коммитим.
videos.json
